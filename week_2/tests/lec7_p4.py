test = {
  'name': 'l7p4',
  'suites': [
    {
      'cases': [
        {
          'code': r"""
          >>> # Please print str_lst. It doesn't seem to be sorted correctly.
          >>> sorted(str_lst) == str_lst
          True
          """
        }   
      ]
    }
  ]
}